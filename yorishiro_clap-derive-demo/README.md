# yorishiro_clap-derive-demo

Pregel cell for the **clap-derive-demo** yorishiro (kami: `bin:clap-derive-demo`,
binary: `clap-derive-demo`).

Per ADR-2605211900 + ADR-2605202200. Generated by `@etzhayyim/yorishiro`
v0.1.0 in **binary-cli** mode.

## Ops

| Op | Summary |
|---|---|
| `encode` | clap-derive encode |
| `decode` | clap-derive decode |

## Runtime requirements

The `clap-derive-demo` binary must be available on the
cell runtime's PATH (or supplied as an absolute path in the kami
manifest at `00-contracts/kami/clap-derive-demo.kami.json`). The cell
verifies this via `shutil.which()` at the start of every invocation
and returns `error: "binary not found on PATH"` if missing.

## Lexicon SSoT

`00-contracts/lexicons/ai/etzhayyim/yorishiro/clap-derive-demo/<op>.json`

## MCP exposure

`kotoba-lang/kotodama-mcp/yorishiro-clap-derive-demo-mcp/` (stdio + Streamable HTTP)

## Regenerate

```bash
yorishiro regen clap-derive-demo
```

## Cell-runner registration

See `cells.toml.fragment` in this directory.

## Claude Desktop / Codex CLI

```json
{
  "mcpServers": {
    "etzhayyim-yorishiro-clap-derive-demo": {
      "command": "node",
      "args": ["/ABSOLUTE/PATH/TO/repo/kotoba-lang/kotodama-mcp/yorishiro-clap-derive-demo-mcp/src/cli.ts"]
    }
  }
}
```

The binary itself does NOT need to be on the MCP host machine if you
intend the MCP server to dispatch to a remote runtime. For the simple
local-binary case shown above, `clap-derive-demo` must be
on the host's PATH.
