# ossekai_aggregate_publisher — Pregel cell

Paired with `20-actors/ossekai/`.
Murakumo node: **issachar**.
This cell handles the default publication mode (AGGREGATE PUBLICATION), surfacing anonymized intel via the AT Protocol `app.bsky.feed.post`.

Resident in Kotoba WASM.
