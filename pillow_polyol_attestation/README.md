# pillow_polyol_attestation — makura L1a (R0 scaffold)

Polyol + catalyst + surfactant + blowing-agent raw-material attestation.
Per ADR-2605261115 §6 L1a. R0 scaffold — `.solve()` raises until R1.

- **Murakumo node**: naphtali
- **Lexicon (output)**: `com.etzhayyim.makura.foamBatchAttestation` (precursor section)
- **Constraints**: G6 (no Hg/Sn catalysts); G12 (bio-content disclosed); G6 (water blowing-agent preferred; HFC requires Council attestation)
